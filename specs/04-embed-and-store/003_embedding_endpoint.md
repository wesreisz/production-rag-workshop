# Embedding Endpoint (Workshop Utility)

**Deliverable:** A public HTTPS endpoint that accepts a text string and returns its 256-dimensional embedding vector from Amazon Bedrock Titan Text Embeddings V2. Workshop participants can `curl` this endpoint to generate embeddings for use in pgvector similarity queries via the RDS Query Editor.

---

## Overview

1. Create a small Lambda handler that takes a JSON body with a `text` field, calls Bedrock, and returns the embedding vector
2. Deploy using the non-VPC `lambda` Terraform module (Bedrock is accessible over the public internet — no VPC needed)
3. Attach a Lambda Function URL with `NONE` auth type for a public HTTPS endpoint
4. Output the URL so participants can immediately `curl` it

---

## Prerequisites

- [ ] Stage 4 Part 2 (002_embedding) is complete and verified
- [ ] Bedrock model access is enabled for `amazon.titan-embed-text-v2:0`

---

## Architecture Context

```
curl POST ──> Lambda Function URL ──> Lambda ──> Bedrock Titan V2
                (public HTTPS)         (no VPC)    (embedding API)
```

This is a workshop utility — not part of the production pipeline. It exists so participants can generate embeddings interactively and paste them into pgvector queries in the RDS Query Editor, bridging the gap between "embeddings are stored" and "here's how similarity search actually works."

---

## API Contract

### Request

```
POST https://<function-url-id>.lambda-url.<region>.on.aws/
Content-Type: application/json
x-api-key: <api key from terraform output>

{
  "text": "What did Wes talk about?"
}
```

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `text` | `string` | Yes | The text to embed. Max ~8,000 tokens for Titan V2. |

| Header | Required | Description |
|--------|----------|-------------|
| `x-api-key` | Yes | API key from `terraform output -raw embed_text_api_key` |

### Response (success)

```json
{
  "embedding": [0.0123, -0.0456, ...],
  "dimensions": 256,
  "model": "amazon.titan-embed-text-v2:0"
}
```

### Response (error)

```json
{
  "error": "text field is required"
}
```

Status code `401` for missing/invalid API key, `400` for missing/invalid input, `500` for Bedrock failures.

### Example usage

```bash
EMBED_URL="<function URL from terraform output>"
API_KEY="<api key from terraform output>"

# Generate embedding
curl -s -X POST "$EMBED_URL" \
  -H "Content-Type: application/json" \
  -H "x-api-key: $API_KEY" \
  -d '{"text": "What did Wes talk about?"}' | python3 -m json.tool

# Use in pgvector similarity search (RDS Query Editor):
# 1. Copy the embedding array from the curl response
# 2. Paste into this query:
#
#    SELECT chunk_id, left(text, 80) AS text_preview,
#           1 - (embedding <=> '<paste_embedding_here>'::vector) AS similarity
#    FROM video_chunks
#    ORDER BY embedding <=> '<paste_embedding_here>'::vector
#    LIMIT 5;
```

---

## Resources

### Part A: Lambda Handler

**Directory structure:**

```
modules/embedding-endpoint/
├── embedding-readme.md
└── src/
    ├── __init__.py
    └── handlers/
        ├── __init__.py
        └── embed_text.py
```

**`embed_text.py` handler logic:**

1. Read `API_KEY` from environment variable and validate the `x-api-key` header from `event["headers"]` — return `401` with `{"error": "unauthorized"}` if missing or wrong
2. Parse the request body from `event["body"]` (Lambda Function URLs pass the HTTP body as a JSON string in `event["body"]`)
3. Validate `text` field is present and non-empty
4. Call Bedrock Titan V2 with `inputText`, `dimensions` from `EMBEDDING_DIMENSIONS` env var (default `256`), `normalize = true`
5. Return JSON response with `statusCode`, `headers` (including CORS `Access-Control-Allow-Origin: *`), and `body`
6. On validation error, return `statusCode: 400`
7. On Bedrock error, return `statusCode: 500`

**Environment variables:**

| Variable | Value |
|----------|-------|
| `EMBEDDING_DIMENSIONS` | `"256"` |
| `API_KEY` | Generated by `random_password` Terraform resource (32 chars, no special characters) |

---

### Part B: Terraform Configuration

**New resources in `infra/environments/dev/main.tf`:**

| Resource | Type | Key Settings |
|----------|------|-------------|
| `random_password.embed_text_api_key` | Random Password | `length` = `32`, `special` = `false`. Simple API key for the endpoint. |
| `module "embed_text_endpoint"` | `lambda` module (non-VPC) | `function_name` = `${var.project_name}-embed-text`, `handler` = `src.handlers.embed_text.handler`, `timeout` = `30`, `source_dir` = embedding-endpoint module path |
| `aws_lambda_function_url.embed_text` | Function URL | `function_name` = module output, `authorization_type` = `NONE` |
| `aws_lambda_permission.embed_text_public_url` | Lambda Permission | `action` = `lambda:InvokeFunctionUrl`, `principal` = `*`, `function_url_auth_type` = `NONE` |
| `aws_lambda_permission.embed_text_public_invoke` | Lambda Permission | `action` = `lambda:InvokeFunction`, `principal` = `*`. Since October 2025, Function URLs require **both** `lambda:InvokeFunctionUrl` and `lambda:InvokeFunction` permissions — without either, the Function URL returns 403 Forbidden. |

**IAM policy for the Lambda:**

```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": ["bedrock:InvokeModel"],
      "Resource": "arn:aws:bedrock:<region>::foundation-model/amazon.titan-embed-text-v2:0"
    }
  ]
}
```

**New output in `infra/environments/dev/outputs.tf`:**

| Output | Value | Description |
|--------|-------|-------------|
| `embed_text_endpoint_url` | `aws_lambda_function_url.embed_text.function_url` | Public URL for generating embeddings |
| `embed_text_api_key` | `random_password.embed_text_api_key.result` | API key (sensitive) for the endpoint |

---

## Implementation Checklist

- [ ] 1. Create `modules/embedding-endpoint/embedding-readme.md` with setup, usage, and similarity search examples
- [ ] 2. Create `modules/embedding-endpoint/src/__init__.py` (empty)
- [ ] 3. Create `modules/embedding-endpoint/src/handlers/__init__.py` (empty)
- [ ] 4. Create `modules/embedding-endpoint/src/handlers/embed_text.py` with handler that validates API key, parses body, validates `text`, calls Bedrock, returns embedding JSON with CORS headers
- [ ] 5. Add `random_password.embed_text_api_key` and `module "embed_text_endpoint"` to `infra/environments/dev/main.tf` using the non-VPC `lambda` module with Bedrock IAM permissions and `API_KEY` env var
- [ ] 6. Add `aws_lambda_function_url.embed_text` resource with `authorization_type = "NONE"`
- [ ] 7. Add `aws_lambda_permission.embed_text_public_url` resource granting `lambda:InvokeFunctionUrl` to principal `*` with `function_url_auth_type = "NONE"`
- [ ] 8. Add `aws_lambda_permission.embed_text_public_invoke` resource granting `lambda:InvokeFunction` to principal `*` (required since October 2025 — both permissions needed for Function URLs)
- [ ] 9. Add `embed_text_endpoint_url` and `embed_text_api_key` outputs to `infra/environments/dev/outputs.tf`
- [ ] 10. Run `terraform apply`
- [ ] 11. Verify: `curl` the endpoint with API key and sample text, confirm a 256-dimensional embedding is returned

---

## Verification

### Step 1: Deploy

```bash
cd infra/environments/dev
terraform apply -var="aurora_master_password=YourSecurePassword123!"
```

### Step 2: Get the endpoint URL and API key

```bash
terraform output embed_text_endpoint_url
terraform output -raw embed_text_api_key
```

### Step 3: Generate an embedding

```bash
EMBED_URL=$(terraform output -raw embed_text_endpoint_url)
API_KEY=$(terraform output -raw embed_text_api_key)

curl -s -X POST "$EMBED_URL" \
  -H "Content-Type: application/json" \
  -H "x-api-key: $API_KEY" \
  -d '{"text": "What did Wes talk about?"}' | python3 -m json.tool
```

Expected: JSON response with `embedding` (array of 256 floats), `dimensions` (256), and `model`.

### Step 4: Use in pgvector similarity search

1. Copy the `embedding` array from the curl response
2. Open the RDS Query Editor, connect to `production-rag-vectordb` / `ragdb`
3. Run:

```sql
SELECT chunk_id, left(text, 80) AS text_preview,
       1 - (embedding <=> '<paste_embedding_here>'::vector) AS similarity
FROM video_chunks
ORDER BY embedding <=> '<paste_embedding_here>'::vector
LIMIT 5;
```

Expected: Results sorted by semantic similarity to "What did Wes talk about?"

### Step 5: Test error handling

```bash
# Missing API key
curl -s -X POST "$EMBED_URL" \
  -H "Content-Type: application/json" \
  -d '{"text": "hello"}' | python3 -m json.tool
```

Expected: `{"error": "unauthorized"}` with status 401.

```bash
# Missing text field
curl -s -X POST "$EMBED_URL" \
  -H "Content-Type: application/json" \
  -H "x-api-key: $API_KEY" \
  -d '{}' | python3 -m json.tool
```

Expected: `{"error": "text field is required"}` with status 400.

---

## Success Criteria

| Criterion | How to verify |
|-----------|---------------|
| Lambda deployed | `aws lambda get-function --function-name production-rag-embed-text` succeeds |
| Lambda is NOT VPC-attached | Function configuration shows no `VpcConfig` subnets |
| Function URL exists | `aws lambda get-function-url-config --function-name production-rag-embed-text` returns a URL |
| Auth type is NONE | Function URL config shows `AuthType: NONE` |
| Public invoke permissions exist | `aws lambda get-policy --function-name production-rag-embed-text` shows both `FunctionURLAllowPublicAccess` (`lambda:InvokeFunctionUrl`) and `FunctionURLInvokeAllowPublicAccess` (`lambda:InvokeFunction`) statements |
| Missing API key returns 401 | `curl` without `x-api-key` header returns status 401 and `{"error": "unauthorized"}` |
| Endpoint returns embedding | `curl` with `x-api-key` header and `{"text": "hello"}` returns 256-dimensional vector |
| CORS headers present | Response includes `Access-Control-Allow-Origin: *` |
| Missing text returns 400 | `curl` with `{}` returns status 400 and error message |
| Embedding matches Bedrock output | Dimensions match `EMBEDDING_DIMENSIONS` env var (256) |
| Terraform outputs exist | `terraform output embed_text_endpoint_url` returns the function URL, `terraform output -raw embed_text_api_key` returns the API key |
